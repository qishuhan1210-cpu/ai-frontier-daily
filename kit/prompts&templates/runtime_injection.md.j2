---

## 运行时注入（由工程填充）

下列内容**不是**协议正文的一部分，仅提供当日数据。

- 早报日期：`{{ date_str }}`
- 覆盖范围文案（与头部语义一致）：`{{ coverage }}`
- 模块 id 列表（`blocks.footer` 键名须与此一致）：`{{ ids_str }}`

### 新闻清单（JSON）

```json
{{ news_json }}
```
