# AI Frontier Daily 依赖列表

# 核心依赖
openai>=1.0.0          # LLM API 调用
requests>=2.31.0       # HTTP 请求（RSS 抓取）
jinja2>=3.1.2          # 模板渲染
pyyaml>=6.0            # YAML 配置文件解析

# 开发依赖
pytest>=8.0.0          # 单元测试
pytest-asyncio>=0.23.0 # async 测试支持
